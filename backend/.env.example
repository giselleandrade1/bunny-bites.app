PORT=4000
JWT_SECRET=change-this-secret-in-production
CORS_ORIGIN=*
DB_PROVIDER=sqlite
DB_FILE=./data/bunnybites.db
# Use postgres in production for persistent data
# DB_PROVIDER=postgres
# DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DBNAME
